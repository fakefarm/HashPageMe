<h2>#about @<%= @username %></h2>
<p>
  <% @about_tweets.each do |t| %>
    <%= link_to tweet_path(t), target: '_blank' do %>
      <%= only_text(t.text) %>
      <% if  t.image.present? %>
        <i class="fa fa-camera-retro"></i>
      <% end %>
    <% end %>
    &nbsp;
  <% end %>
</p>
